import React from "react";

const page = () => {
  // グルーピング：ルートグループ((xxx))
  // auth配下の子供が増えたり減ったりしない場合等(loginなど)に利用
  // http://localhost:3000/login
  return <div>ログイン</div>;
};

export default page;
