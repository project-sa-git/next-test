import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
  title: "お問い合わせ",
  description: "お問い合わせページです",
};

export const page = () => {
  return <div>コンタクトpage</div>;
};

export default page;
